# Render static site — SPA fallback so /about, /dashboard, etc. reload correctly
services:
  - type: web
    name: gilliom-frontline-digital
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
